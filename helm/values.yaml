replicaCount: 2

image:
  repository: cloud-native-java-app
  tag: v1

service:
  type: NodePort
  port: 80
  targetPort: 8080

resources:
  requests:
    memory: "256Mi"
    cpu: "200m"
  limits:
    memory: "512Mi"
    cpu: "500m"

env:
  APP_ENV: base
  LOG_LEVEL: INFO
